import React from 'react';

export const VisibilityOn = (): JSX.Element => (
  <i className='fas fa-eye-slash' style={{ fontSize: '1.35rem' }} />
);
export const VisibilityOff = (): JSX.Element => (
  <i className='fas fa-eye' style={{ fontSize: '1.35rem' }} />
);
